---
import { SITE } from "$/config";
import ModeLabel from "./ModeLabel.svelte";
import NetlifyIdentity from "./NetlifyIdentity.svelte";

const year = new Date().getFullYear();
---

<footer class="footer">
  <nav class="nav">
    <div>
      {year} &copy; Copyright notice | <a href={SITE.githubUrl}
        title={`${SITE.name}'s Github URL'`}>{SITE.name}</a>
      is built with <a href="https://astro.build/">Astro</a>
    </div>
  </nav>
</footer>
<style>
  .footer {
    @apply py-6 border-t;
  }
</style>
